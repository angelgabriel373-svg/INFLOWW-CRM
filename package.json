{
  "name": "infloww-crm",
  "version": "1.0.0",
  "private": true,
  "description": "Clon de Infloww — CRM de gestion de modelos y chatters (OFM). 100% open source, coste cero.",
  "scripts": {
    "install:all": "npm install && npm --prefix server install && npm --prefix client install",
    "setup": "npm run install:all && npm --prefix server run db:setup",
    "dev": "concurrently -n SERVER,CLIENT -c green,cyan \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "build": "npm --prefix client run build",
    "start": "npm --prefix server start"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
